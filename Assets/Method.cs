/*

using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Method : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()                // Eljárás
    {
        WriteNumbersFrom1(15);
        Debug.Log("hhhhh");
        WriteNumbersFrom1(8);
    }

    void WriteNumberSeguence(int from, int to)
    {
        for (int i = from;  <= to; i++)
            Debug.Log(i);
    }
    void.WriteNumbersFrom1(int lenght)
    {
        for (int i = 1; i <= lenght; i++)
            Debug.Log(i);
    }

    float Power(float baseNumber, int exponent)
    {
        float result = 1;
        for (int i = 0; i < exponent; i++)
            result *= baseNumber;

        return baseNumber;
    }

    float Absolute()

    }
}
*/